{% extends 'base.html.twig' %}
{% block stylesheets %}
	{# {{ encore_entry_link_tags('app') }} #}
	<link href="{{asset('css/header.css')}}" rel="stylesheet" media="all">
	<link href="{{asset('css/paypal.css')}}" rel="stylesheet" media="all">
{% endblock %}
{% block body %}
		<!--Start Image-->
		{# <div class="img-bg ms-5">
					<img src="images\LogIn.png" alt="">
				</div> #}
		<!--End Image-->
		<!--Start Content-->
		<div class="container-bill">
			<div class="col-login">
				<h3 class="title">Login</h3>
				{{ form_start(paypal) }}
				{# <form method="post"> #}
				{# {% if error %}
										<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
									{% endif %} #}
				{# 
									{% if app.user %}
										<div class="mb-3">
											You are logged in as
											{{ app.user.userIdentifier }},
											<a href="{{ path('logout_page') }}">Logout</a>
										</div>
									{% endif %} #}

				<div class="inputBox"> <span>{{ form_label(paypal.email) }}
						:</span>
					{{ form_widget(paypal.email) }}
				</div>
				<div class="inputBox">
					<span>{{ form_label(paypal.password) }}:</span>
					{{ form_widget(paypal.password) }}
				</div>
				<div class="remember-login">
					<label><input type="checkbox" name="" class="me-2">Remember Login</label>
				</div>
				<input
				type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
				{# Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
										See https://symfony.com/doc/current/security/remember_me.html
				
									<div class="checkbox mb-3">
										<label>
											<input type="checkbox" name="_remember_me">
											Remember me
										</label>
									</div> #}
				<div>
					<input type="submit" value="Login" class="submit-btn">
				</div>
				{# </form> #}

				<div class="input-form text-center">
					<p>Do not have an account?
						<a class="sign In ms-3" href="{{path("signIn")}}">SignIn</a>
					</p>
				</div>
			</div>
		</div>
		<!--End Content-->
{% endblock %}
